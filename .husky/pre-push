#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Biome (lint + format)..."
bun run lint || exit 1

echo "🧠 Type-checking (backend + admin + storefront)..."
bun run tsc || exit 1

echo "✅ Pre-push checks passed."
